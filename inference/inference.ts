// The following bindings examplify how inference works
// in TypeScript; hover over the values without annotations
// and you see that TS infers the type based on the initialiser

let num1 = 2; // TS type: number
let str2 = "string"; // TS type: string
let bool2 = true; // TS type: boolean
let _Date2 = new Date(); // TS type: Date
let obj2 = {}; // TS type: {}
